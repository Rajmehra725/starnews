/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.hawkcentral.com",
      "cdn.cnn.com",
      "images.unsplash.com"
    ],
  },
};

module.exports = nextConfig;